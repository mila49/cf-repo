# Optional: Weights & Biases experiment tracking
# W&B is enabled by default. Set WANDB_ENABLED=false to disable.
# export WANDB_ENABLED=false
export WANDB_API_KEY="your-api-key"
export WANDB_ENTITY="your-entity"
export WANDB_PROJECT="cf-repo"
